extends layout

block content
  h1 #{title}: #{bookinstance.id}
  p= test

    p Do you really want to delete this bookinstance?

    form(method='POST' action='')
      div.form-group
      input#book_instanceid.form-control(type='hidden', name='book_instanceid', required='true', value=bookinstance._id )

      button.btn.btn-primary(type='submit') Delete